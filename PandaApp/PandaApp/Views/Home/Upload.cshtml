@model PandaApp.Models.Media


@{
    ViewBag.Title = "Upload";
}

@using (Html.BeginForm("Upload", "Home", FormMethod.Post, new { enctype = "multipart/form-data" })) 
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>Subtitle upload</h4>
        <hr />
        @Html.ValidationSummary(true)

        <div class="form-group">
            @Html.LabelFor(model => model.Title, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.TextBoxFor(model => model.Title, new { @class = "form-control" })
                @Html.ValidationMessageFor(model => model.Title)
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.ID, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.TextBoxFor(model => model.ID, new { @class = "form-control" })
                @Html.ValidationMessageFor(model => model.ID)
            </div>
        </div>
        <!-- New upload field starts-->  <!--TODO send file somewhere!-->
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <form id="Form1" method="post" enctype="multipart/form-data" runat="server" >
                    <input type=file id=file name=file />
                    <br>

                    <input type="submit" id="Submit1" value="Upload" class="btn btn-default" />
                </form>
                </div>
            </div>
        <!-- new upload field ends-->

     <!--        this is the old "create" 
 
         <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </div>
         
     -->


    </div>
}
    <div>
        @Html.ActionLink("Back to List", "Index")
    </div>




 