<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title - Project Panda</title>
    @Styles.Render("~/Content/css")
    @Scripts.Render("~/bundles/modernizr")

</head>
<body>
    @if (Request.IsAuthenticated)
    {
        <a href="javascript:document.getElementById('logoutForm').submit()">Log off</a>
    }
        <div class="navbar">
            <h1>@Html.ActionLink("Panda", "Index", "Home")</h1>
            <ul class="navbar-nav">
                <li class="btn">@Html.ActionLink("Browse", "Index", "Home")</li>
                <li class="btn">@Html.Partial("_LoginPartial")</li>
                <li class="btn">@Html.ActionLink("Requests", "Requests", "Home")</li>
                <li class="btn">@Html.ActionLink("Upload", "Upload", "Home")</li>
                <li class="btn">@Html.ActionLink("FAQ", "FAQ", "Home")</li>
            </ul>
        </div>
            

    <div class="container body-content">
        @RenderBody()
        <hr />
        <footer class="footer-left">
            <p>support@panda.com</p>
        </footer>
        <footer class="footer-right">
            <p>&copy; Panda @DateTime.Now.Year</p>
        </footer>
    </div>
    
    

    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/bootstrap")
    @RenderSection("scripts", required: false)
</body>
</html>
